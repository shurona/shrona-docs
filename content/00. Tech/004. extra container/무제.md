docker compose up -d --force-recreate caddy

  

journalctl -u jibsa-server -f # 실시간 팔로우 (docker logs -f 대응)

journalctl -u jibsa-server -n 100 # 최근 100줄

journalctl -u jibsa-server --since "10 min ago" # 최근 10분

tail -f /data/jibsa-service/jibsa-server/logs/jibsa-server.log # 파일 쪽 실시간