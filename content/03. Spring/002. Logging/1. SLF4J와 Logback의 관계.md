---
tags:
  - logging
  - slf4j
  - logback
---
# 1. SLF4J와 Logback의 관계

> [[배포 후 로그 관리를 해보자]] 로드맵의 1단계

## SLF4J는 API, Logback은 구현체

SLF4J(Simple Logging Facade for Java)는 이름 그대로 **파사드(Facade)**, 즉 인터페이스만 정의한 API이다. 실제로 로그를 어디에 어떤 형식으로 쓸지는 SLF4J가 하지 않고, 뒤에 붙는 **구현체**가 담당한다. Logback은 그 구현체 중 하나이다.

```
내 코드 → SLF4J (API) → Logback (구현체) → 콘솔 / 파일 / ...
```

JDBC에 비유하면 이해가 쉽다. 코드는 JDBC 인터페이스만 바라보고, 실제 DB가 PostgreSQL이든 MySQL이든 드라이버만 바꾸면 되는 것과 같은 구조이다. 코드는 SLF4J만 바라보고, 구현체를 Logback에서 Log4j2로 바꿔도 코드 수정이 없다.

### 왜 이렇게 나눴는가

라이브러리 생태계 때문이다. 내 애플리케이션이 쓰는 수많은 라이브러리(Spring, Hibernate, HikariCP...)도 각자 로그를 남긴다. 만약 각 라이브러리가 특정 구현체에 직접 의존하면, 내 앱에 로깅 구현체가 여러 개 섞이고 설정도 제각각이 된다. 모두가 SLF4J라는 공통 인터페이스에만 의존하면, 최종 애플리케이션이 구현체를 하나만 선택해서 전체 로그를 한 곳에서 제어할 수 있다.

## Spring Boot는 무엇을 넣어주는가

`spring-boot-starter-web`을 쓰면 `spring-boot-starter-logging`이 딸려 오고, 그 안에 이런 것들이 들어 있다. (mommytalk-server에서 `./gradlew dependencies --configuration runtimeClasspath`로 직접 확인한 결과)

```
spring-boot-starter-logging:3.4.5
 ├── ch.qos.logback:logback-classic:1.5.18   ← 구현체 (logback-core 포함)
 │    └── org.slf4j:slf4j-api:2.0.17         ← API
 ├── org.slf4j:jul-to-slf4j:2.0.17           ← 브릿지 1
 └── org.apache.logging.log4j:log4j-to-slf4j:2.24.3  ← 브릿지 2
```

### 브릿지(bridge)의 역할

세상 모든 라이브러리가 SLF4J를 쓰는 것은 아니다. JDK 내장 로거(java.util.logging)를 쓰는 것도 있고, Log4j2 API를 쓰는 것도 있다. 브릿지는 이런 "다른 로깅 API로 쓰인 로그"를 가로채서 SLF4J로 흘려보내는 어댑터이다.

- `jul-to-slf4j`: java.util.logging → SLF4J
- `log4j-to-slf4j`: Log4j2 API → SLF4J

덕분에 어떤 라이브러리가 어떤 로깅 API를 쓰든 **최종적으로 전부 Logback 하나로 모인다.** 그래서 logback 설정 하나로 앱 전체(외부 라이브러리 포함)의 로그를 제어할 수 있는 것이다.

## @Slf4j가 실제로 하는 일

Lombok의 `@Slf4j`는 컴파일 시점에 아래 한 줄을 클래스에 넣어주는 것이 전부이다.

```java
private static final org.slf4j.Logger log =
    org.slf4j.LoggerFactory.getLogger(현재클래스.class);
```

즉 `log.info(...)`를 호출하면 SLF4J의 Logger 인터페이스를 호출하는 것이고, 런타임에 클래스패스에 있는 Logback이 실제 출력을 담당한다.

## 핵심 개념 4가지

Logback 설정을 이해하려면 이 4가지 개념만 잡으면 된다.

### ① Logger - 이름 계층 구조

Logger는 보통 클래스의 풀네임(`com.shrona.mommytalk.kakao.KakaoMessageScheduler`)을 이름으로 가지며, 패키지 경로를 따라 **계층 구조**를 이룬다.

```
root
 └── com
      └── com.shrona
           └── com.shrona.mommytalk
                └── com.shrona.mommytalk.kakao
```

레벨을 지정하지 않은 Logger는 **부모의 레벨을 상속**받는다. 그래서 `com.shrona.mommytalk`에 DEBUG를 주면 그 하위 패키지 전체가 DEBUG가 되고, root에 INFO를 주면 나머지 전부(외부 라이브러리 포함)는 INFO가 된다. "내 코드는 자세히, 라이브러리는 조용히"가 이 상속 구조로 가능해진다.

### ② Level - 심각도 필터

TRACE < DEBUG < INFO < WARN < ERROR 순서로 심각하다. Logger에 INFO가 설정되면 INFO 이상(INFO, WARN, ERROR)만 통과하고 DEBUG, TRACE는 버려진다.

> 실제 사례: mommytalk-server는 레벨 설정이 없어서 전부 기본값 INFO였고, 코드에 있던 `log.debug` 8건은 한 번도 출력된 적이 없었다.

### ③ Appender - 어디로 쓸 것인가

출력 목적지이다. 하나의 Logger에 여러 Appender를 붙일 수 있다.

- `ConsoleAppender`: 콘솔(stdout)
- `RollingFileAppender`: 파일 + 롤링(날짜/크기 기준으로 파일 분리)
- 그 외: Logstash TCP 전송, DB 등

> 실제 사례: charity 프로젝트는 운영 프로필에서 CONSOLE과 FILE 두 Appender를 동시에 사용한다. 날짜마다 로그 파일이 잘려서 생기는 것은 `RollingFileAppender`의 `TimeBasedRollingPolicy`(`%d{yyyy-MM-dd}` 패턴 + gzip + 30일 보관) 때문이다.

### ④ Encoder / Pattern - 어떤 형식으로 쓸 것인가

로그 한 줄의 포맷을 결정한다.

```
%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
```

| 패턴 | 의미 |
|------|------|
| `%d{...}` | 시각 |
| `[%thread]` | 스레드 이름 |
| `%-5level` | 레벨 (5칸 왼쪽 정렬) |
| `%logger{36}` | 로거 이름 (최대 36자로 축약) |
| `%msg%n` | 메시지 + 줄바꿈 |

JSON 구조화 로깅은 이 Encoder를 PatternLayout 대신 JSON Encoder로 바꾸는 것이다. (→ 5단계에서 다룬다)

## 실무에서 바로 쓰는 두 가지 습관

### 문자열 연결 대신 `{}` 플레이스홀더

```java
log.debug("user: " + user);   // 나쁨: DEBUG가 꺼져 있어도 문자열 연결 비용 발생
log.debug("user: {}", user);  // 좋음: 레벨 통과 시에만 문자열 생성
```

레벨 체크는 메서드 안에서 일어나므로, 문자열 연결 방식은 로그가 버려질 때도 연결 비용을 치른다.

### 예외는 마지막 인자로 (플레이스홀더 없이)

```java
log.error("결제 실패: {}", orderId, ex);   // 좋음: 스택트레이스 전체 출력
log.error("결제 실패: " + ex.getMessage()); // 나쁨: 메시지만 남고 스택트레이스 유실
```

마지막 인자가 Throwable이면 SLF4J가 자동으로 스택트레이스를 붙여서 출력한다. `ex.getMessage()`만 찍으면 어디서 터졌는지 알 수 없다.

## 직접 확인해보기

```bash
# 내 프로젝트에 어떤 로깅 라이브러리가 들어있는지 확인
./gradlew -q dependencies --configuration runtimeClasspath | grep -i "slf4j\|logback"
```

## 정리

- 코드가 의존하는 것은 SLF4J(API)뿐이고, 실제 출력은 Logback(구현체)이 한다.
- Spring Boot가 starter-logging으로 Logback + 브릿지들을 자동 구성해 주기 때문에, 모든 라이브러리의 로그가 Logback 한 곳으로 모인다.
- 설정의 재료는 Logger(누가) / Level(얼마나) / Appender(어디로) / Encoder(어떤 형식으로) 4가지이다.
- 다음 단계: yaml `logging:` 설정만으로 레벨과 파일 출력을 제어해 본다. → 2단계
